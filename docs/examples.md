# Examples

Standalone HTML templates for common layouts. Open a card for the live page. For every element in one place, use the [Kitchen sink](/kitchen-sink/).

<ExamplesGallery />

## Source

Templates live in `src/docs/examples/`. Regenerate card screenshots with `npm run examples:shots`.
