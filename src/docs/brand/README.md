# Docs brand mark

Canonical mark: **horned window** (blue on white seal). Matches product `--bf-blue` (`#0056b3`).

| File | Use |
| ---- | --- |
| `logo.svg` | Master mark |
| `logo-32.png` | Raster preview |

Published in `docs/public/` as `logo.svg`, `favicon.svg`, PNG/ICO sizes, `apple-touch-icon.png`, `safari-pinned-tab.svg`, `site.webmanifest`.
